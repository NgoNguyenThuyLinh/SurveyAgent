'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    const tableInfo = await queryInterface.describeTable('questions');
    
    // Add is_ai_generated column if it doesn't exist
    if (!tableInfo.is_ai_generated) {
      await queryInterface.addColumn('questions', 'is_ai_generated', {
        type: Sequelize.BOOLEAN,
        allowNull: false,
        defaultValue: false,
        comment: 'Whether this question was generated by AI',
        after: 'display_order'
      });
      console.log(' Added is_ai_generated column to questions');
    } else {
      console.log(' is_ai_generated column already exists in questions');
    }
  },

  down: async (queryInterface, Sequelize) => {
    await queryInterface.removeColumn('questions', 'is_ai_generated');
  }
};
